# Session Context

## Session updates
- 2026-02-26: Reviewed `/docs/CONTEXT` files to assess whether personality/context docs are worth maintaining. Current status: yes, these docs are high-value for communication quality, scoping, and decision support in future sessions. Next steps: keep these files as a standing context pack; optionally reduce overlap between `GLOBAL-INSTRUCTIONS.md` and `about-me.md` over time.
- 2026-02-26: Removed `/docs/CONTEXT/GLOBAL-INSTRUCTIONS.md` from the template to avoid duplicate persona/instruction sources. Current status: context pack now uses focused files (`about-me.md`, `brand-voice.md`, `working-style.md`) only. Next steps: keep these three files synchronized as the single source of truth.
- 2026-04-23: Project direction clarified from `/Users/russ/Downloads/research_site_triage_process.md`. Current status: this repo is for an internal research website triage workbench, not a public-facing research directory build. The MVP should support one pasted-in request at a time and produce an internal triage brief plus a routing-focused follow-up email.
- 2026-04-23: Built the first local MVP as a Vite + React app. Current status: users can paste request text, generate an editable triage workspace, adjust the recommendation and meeting notes, and copy Markdown exports for the internal brief and follow-up email. Validation: `npm run check`, `npm run build`, and local screenshots at `output/playwright/desktop-home.png` and `output/playwright/mobile-home.png`. Next steps: refine parsing heuristics against real request examples and decide whether to add true AI-assisted drafting later.
- 2026-04-23: Improved form parsing for real intake exports that use question lines plus answer blocks rather than `Label: value` pairs. Current status: the parser now skips helper text, note blocks, and prompt copy, and it extracts core fields such as title, faculty, purpose, audience, ownership, structure, and features from multi-line pasted forms. It also bases routing heuristics on extracted answers instead of the whole raw form text.
- 2026-04-23: Added an OpenAI-backed analysis path using `gpt-5.4-mini` through the Responses API. Current status: the app now supports both a rule-based draft and an LLM-assisted draft, with the API key stored locally in browser `localStorage` for localhost MVP use only. Next steps: test the model output against a few real requests and decide whether to keep the current schema/prompt or refine the recommendation logic further.
- 2026-04-23: Refactored the LLM path to the Vercel deployment shape. Current status: the browser now calls `/api/analyze`, the OpenAI request runs server-side in `api/analyze.ts`, and `OPENAI_API_KEY` is expected in server environment variables instead of browser storage. Local smoke tests confirmed the route returns a helpful error when the key is missing and a 401 from OpenAI when a dummy key is supplied.
